name: CI/CD - Frontend Staging Environment

on:
  push:
    branches: [ stage ]
  workflow_dispatch:

jobs:
  validate-and-build:
    name: Build & Validate Frontend Staging
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Frontend Build Verification
        run: |
          npm install
          npm run build

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate Traceable Version Tag
        id: vars
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          TIMESTAMP=$(date +'%Y%m%d%H%M')
          echo "TAG=stage-${SHORT_SHA}-${TIMESTAMP}" >> $GITHUB_OUTPUT

      - name: Build & Push Staging Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.prod
          build-args: |
            VITE_API_URL=http://13.203.161.35:30011
            VITE_SOCKET_URL=http://13.203.161.35:30011
          push: true
          tags: |
            dasujandb/support-chat-frontend:${{ steps.vars.outputs.TAG }}
            dasujandb/support-chat-frontend:stage-latest

  deploy-stage:
    name: Deploy to Staging (Manual Approval Trigger)
    needs: validate-and-build
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Manual Deployment
        run: echo "Manual deployment trigger executed for Frontend Staging environment."
