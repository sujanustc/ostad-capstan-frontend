name: CI/CD - Frontend Dev Environment

on:
  push:
    branches: [ dev ]
  workflow_dispatch:

jobs:
  validate-and-build:
    name: Build & Validate Frontend Dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Frontend Build Verification
        run: |
          npm install
          npm run build

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate Traceable Version Tag
        id: vars
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          TIMESTAMP=$(date +'%Y%m%d%H%M')
          echo "TAG=dev-${SHORT_SHA}-${TIMESTAMP}" >> $GITHUB_OUTPUT

      - name: Build & Push Dev Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.dev
          push: true
          tags: |
            dasujandb/support-chat-frontend:${{ steps.vars.outputs.TAG }}
            dasujandb/support-chat-frontend:dev-latest

  deploy-dev:
    name: Deploy to Dev (Manual Approval Trigger)
    needs: validate-and-build
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Manual Deployment
        run: echo "Manual deployment trigger executed for Frontend Dev environment."
